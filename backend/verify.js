const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { console.log('=== DATOS EN RENDER ==='); const users = await prisma.user.findMany({ select: { name: true, email: true } }); console.log('\nUsuarios:', users); const projects = await prisma.project.findMany({ select: { name: true } }); console.log('\nProyectos:', projects); } main().catch(console.error).finally(() => prisma.$disconnect());
